/// Off-web there is no HTML splash, because the app is already on screen by
/// the time Dart runs. Doing nothing here is the correct behaviour, not a
/// placeholder for something missing.
void dismissBootSplash() {}
